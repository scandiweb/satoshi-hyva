# Gitingore ignores all files in the root directory
# We need to unignore all, and then ignore the files we want to ignore
# We want to keep only app/code, app/design, and vendor/magento

!*

/.git/
/.vscode/
/.idea/
/node_modules/

/.cursor/README.md
/.cursor/.gitignore*
/.cursor/.cursorignore*
/.cursor/.git/

# Unignore specific directories we want to keep

!/app/
/vendor/*
!/vendor/magento/
!/vendor/hyva-themes/
# !!! add other important packages here manually

# Exclude tests
/dev/tests/*
**/Tests/*
**/Test/*